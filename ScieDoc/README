#######################
# For everybody
#######################
# Download
git clone git@mesonh.aero.obs-mip.fr:/gitrepos/MNH-DOC.git 

#######################
# For contributors
#######################
git status
git branch
git tag

# Change branch
git branch -v -a
git checkout -b MNH-53-branch origin/MNH-53-branch
git checkout  MNH-53-branch

# Change source
git diff
git add 3-6-MixedMicrophysics.tex
git commit -m 'Jean-Pierre 14/10/2016: updating version name'

# Upload changes
git tag V4.10.0
git push
git push --tag
